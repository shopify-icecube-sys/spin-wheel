query CartInput {
  cart {
    lines {
      id
      cost {
        subtotalAmount {
          amount
        }
      }
      merchandise {
        ... on ProductVariant {
          sku
        }
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "wheelify", key: "percentage") {
      value
    }
  }
}
